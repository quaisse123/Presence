FONCTIONNEL — Compléter les fonctionnalités inachevées
4. Finaliser la création de session (professeur)
Backend : implémenter SessionManager.createSession() (retourne null actuellement) + endpoint POST /api/sessions
Frontend : remplacer le bouton "Create Session" qui affiche "coming soon" par un vrai formulaire

5. Ajouter la fermeture de session
Endpoint PUT /api/sessions/{id}/close (met endTime à maintenant)

6. Implémenter la vraie vérification GPS
isLocationValid() retourne true en dur → implémenter la formule Haversine (distance entre le scan et la salle ≤ rayon)

7. Implémenter la règle LATE
getAttendanceStatus() retourne toujours PRESENT → ajouter une période de grâce (ex: 10 min après le début = LATE)

8. Implémenter la vérification d'appartenance au groupe
ActivationMembershipHelper.belongsToSelectedGroup() retourne true en dur → brancher sur une vraie source (liste d'étudiants par groupe)

🟡 QUALITÉ & PRÉSENTATION — Pour un beau repo
9. Nettoyer le code mort / TODO
SessionManager : createSession(), updateSession(), deleteSession() retournent null/vide
AttendanceManager : createAttendance(), getAttendanceById(), etc. retournent null
Supprimer ou implémenter ces méthodes inutilisées

10. Corriger le test Flutter obsolète
Le widget_test.dart teste un compteur qui n'existe plus dans ton app. Soit le supprimer, soit le remplacer par un test simple (ex: la page Login s'affiche).

11. Durcir la sécurité (au minimum)
CORS : remplacer allowedOrigins("*") par l'URL réelle de ton frontend
Secret JWT : le déplacer dans une variable d'environnement (pas en dur dans le fichier)
Rôles par endpoint : dans SecurityConfig, restreindre (ex: seul un PROFESSEUR peut créer un cours)

12. Améliorer le README
Ton README est déjà très complet ✅. Ajoute juste :

Un capture d'écran de l'app (login, dashboard prof, dashboard étudiant)
Un badge "build passing" si tu veux
Une section "Fonctionnalités à venir" (roadmap)

🟢 BONUS — Pour un repo pro
13. Ajouter un .gitignore racine
Il n'y a pas de .gitignore à la racine du projet (seulement dans backend et frontend). Ajoute-en un pour ignorer les fichiers système (.DS_Store, etc.).

14. Ajouter une licence
Ajoute un fichier LICENSE (MIT par exemple) pour préciser les droits d'utilisation.

15. Ajouter un CHANGELOG.md
Documente les versions/évolutions.

